//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    semi: true,
    singleQuote: false,
    trailingComma: "all",
    tabWidth: 4,
    printWidth: 120,
    bracketSpacing: true,
    endOfLine: "lf",
};

export default config;
